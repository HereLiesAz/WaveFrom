# The simulator backend, transport, and the DSP (dsp.py) run on the Python
# standard library alone — the pod and its tests work with nothing installed.
#
# For real hardware / faster + higher-resolution DSP, install:
#   numpy        # fast covariance + FFT, and MUSIC high-resolution DoA
#   pyrtlsdr     # RTL-SDR capture (rtlsdr backend)
# The KrakenSDR backend additionally needs a running Heimdall DAQ (TCP :5000);
# the WiFi-CSI backend needs a nexmon_csi NIC streaming CSI UDP (:5500).
#
# numpy
# pyrtlsdr
